const menu=document.querySelector('.menu');const links=document.querySelector('.links');if(menu)menu.addEventListener('click',()=>links.classList.toggle('open'));document.querySelectorAll('.links a').forEach(link=>link.addEventListener('click',()=>links.classList.remove('open')));document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());const nav=document.querySelector('nav');if(nav){window.addEventListener('scroll',()=>{nav.classList.toggle('scrolled',window.scrollY>40)})}const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')})},{threshold:.1,rootMargin:'0px 0px 20% 0px'});document.querySelectorAll('.reveal').forEach(el=>io.observe(el));const loadStyle=href=>{const stylesheet=document.createElement('link');stylesheet.rel='stylesheet';stylesheet.href=href;document.head.append(stylesheet)};if(document.querySelector('.updates-section'))loadStyle('updates.css');if(document.querySelector('.sponsor-strip'))loadStyle('partner-extra.css');if(document.title.startsWith('The Team')){loadStyle('team-photo.css');const teamMain=document.querySelector('main');const oldTeamHero=document.querySelector('.inner-hero');const teamCover=document.createElement('section');teamCover.className='team-cover';teamCover.innerHTML='<div class="team-cover-overlay"></div><div class="team-cover-copy"><div class="eyebrow mono">Team Velocity / VNIT Nagpur</div><h1 class="display">The people<br>behind the <em>build.</em></h1><p>Scroll to meet the team.</p></div><span class="team-cover-scroll mono">Scroll to reveal ↓</span>';teamMain.insertBefore(teamCover,teamMain.firstChild);if(oldTeamHero)oldTeamHero.remove();const roster=document.querySelector('.team-section');if(roster)roster.id='team';window.addEventListener('scroll',()=>{const progress=Math.min(window.scrollY/(window.innerHeight*.65),1);teamCover.style.opacity=String(1-progress*.72);teamCover.style.transform=`scale(${1-progress*.04})`},{passive:true})}
