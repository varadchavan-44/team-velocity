{
  "name": "team-velocity",
  "version": "1.0.0",
  "private": true,
  "description": "Team Velocity VNIT site with Vercel-hosted backend for team roster + homepage content",
  "scripts": {
    "seed": "node scripts/run-sql.js schema.sql && node scripts/run-sql.js seed.sql",
    "hash-password": "node scripts/hash-password.js"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.0",
    "@vercel/postgres": "^0.10.0",
    "bcryptjs": "^2.4.3",
    "cookie": "^0.6.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "dotenv": "^16.4.5"
  }
}
